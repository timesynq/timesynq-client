import { NavBar } from "@/components/nav-bar";

export const Room = () => {

    return (
        <>
            <NavBar />
            <main className="flex flex-col items-center justify-center">
                <p>Room</p>
            </main>
        </>
    );
}